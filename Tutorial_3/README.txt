The code provided in this tutorial, is a model answer for the first three
questions for the first OpenMP tutorial. To compile the sources use the
following command:
	g++ -std=c++11 -fpermissive -c OpenMPExcercises.cpp
